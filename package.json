{
  "name": "svelte-recipe-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd recipe-components && npm install && npm run build && npm pack && cd ../recipe-app && npm install && npm run build"
  }
}
